export function CodeDatabase({codes,onRemove}:{codes:string[];onRemove:(index:number)=>void}){return <section className="panel"><div className="panel-head"><div><span className="eyebrow">SOURCE OF TRUTH</span><h2>Composite code database</h2></div><span className="count">{codes.length} objects</span></div><div className="code-list">{codes.map((code,index)=><div className="code-row" key={`${code}-${index}`}><code>{code}</code><button className="ghost danger" onClick={()=>onRemove(index)}>×</button></div>)}</div></section>}
