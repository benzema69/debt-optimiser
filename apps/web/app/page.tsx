"use client";
import{useEffect,useMemo,useState}from"react";import{ActualsPanel}from"../components/ActualsPanel";import{CodeDatabase}from"../components/CodeDatabase";import{DailyTrajectory}from"../components/DailyTrajectory";import{LoadProfile}from"../components/LoadProfile";import{MetricCard}from"../components/MetricCard";import{MonthlyMatrix}from"../components/MonthlyMatrix";import{Sandbox}from"../components/Sandbox";import{StatusBadge}from"../components/StatusBadge";import{getSeed,optimizeCodes}from"../lib/api";import type{OptimizationResult}from"../lib/types";
const money=(n:number)=>new Intl.NumberFormat("fr-CH",{maximumFractionDigits:0}).format(n);
export default function Home(){const[codes,setCodes]=useState<string[]>([]),[result,setResult]=useState<OptimizationResult|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null);async function solve(next:string[]){setLoading(true);setError(null);try{setResult(await optimizeCodes(next))}catch(e){setError(String(e))}finally{setLoading(false)}}useEffect(()=>{(async()=>{try{const seed=await getSeed();setCodes(seed.codes);await solve(seed.codes)}catch(e){setError(String(e));setLoading(false)}})()},[]);async function applyCode(code:string){const next=[...codes,code];setCodes(next);await solve(next)}async function remove(index:number){const next=codes.filter((_,i)=>i!==index);setCodes(next);await solve(next)}const metrics=result?.metrics,january=metrics?.monthly_totals["2027-01"]??0,weekly=metrics?metrics.global_mt/metrics.days_in_window*7:0,issues=useMemo(()=>result?.issues??[],[result]);return <main><header className="topbar"><div className="brand"><div className="brand-mark">DO</div><div><b>Debt Optimiser</b><span>Discrete Capital Cleanup Engine</span></div></div><StatusBadge ok={Boolean(result?.valid)} label={loading?"SOLVING":result?.valid?"ENGINE VALID":"ENGINE ERROR"}/></header><section className="hero"><div><span className="eyebrow">GLOBAL CLEANUP PROTOCOL</span><h1>Zero liability.<br/><em>Before February.</em></h1><p>Immutable source codes in. Integer-unit optimization out. No fractional units, no hidden spreadsheet logic.</p></div><div className="zero-card"><span>FEBRUARY LIABILITY</span><strong>{result?.valid?"CHF 0.00":"INVALID"}</strong><small>Required state on 01 Feb 2027</small></div></section>{error?<div className="error-box"><b>API unavailable</b><p>{error}</p></div>:null}{issues.length?<div className="error-box">{issues.map(i=><p key={`${i.code}-${i.error}`}><b>{i.error}</b> {i.detail}</p>)}</div>:null}{metrics?<section className="metrics-grid"><MetricCard label="GLOBAL MT" value={`CHF ${money(metrics.global_mt)}`} hint="live checksum" accent="violet"/><MetricCard label="PEAK MONTH" value={`CHF ${money(metrics.peak_monthly)}`} hint="solved maximum"/><MetricCard label="AVERAGE / DAY" value={`CHF ${metrics.average_per_day.toFixed(2)}`} hint={`${metrics.days_in_window} calendar days`}/><MetricCard label="AVERAGE / WEEK" value={`CHF ${weekly.toFixed(2)}`} hint="global average"/><MetricCard label="JANUARY" value={`CHF ${money(january)}`} hint="landing month" accent="good"/></section>:null}{result?.valid?<MonthlyMatrix result={result}/>:null}{metrics?<DailyTrajectory metrics={metrics}/>:null}{metrics?<LoadProfile metrics={metrics}/>:null}<div className="two-col"><CodeDatabase codes={codes} onRemove={remove}/><Sandbox codes={codes} onApply={applyCode}/></div>{metrics?<ActualsPanel planned={metrics.global_mt}/>:null}<footer><span>Source → Parse → Validate → Optimize → Render</span><span>{result?.solver??"no solver"}</span></footer></main>}
